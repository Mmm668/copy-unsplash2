import React from 'react';
import styled from "styled-components";

const ColletionDetail = (props) => {

    return (
        <Wrapper>
           ColletionDetail
        </Wrapper>
    )
};

const Wrapper = styled.div`
`;

export default ColletionDetail;